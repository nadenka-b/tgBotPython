# KFU Admissions Analysis Bot

## 📋 Описание

Телеграм-бот для анализа и парсинга данных о поступлении в КФУ (Казанский Федеральный Университет). Бот собирает информацию о программах, количестве поступающих, мест в конкурсе и других показателях, затем анализирует и предоставляет статистику через интерфейс Telegram.

## ✨ Основные возможности

- **Парсинг данных**: Автоматическое получение данных со страницы абитуриентов КФУ
- **Анализ статистики**: Вычисление количества поступающих на место, анализ популярности направлений
- **Фильтрация данных**: Динамическая фильтрация программ по различным критериям
- **Экспорт отчётов**: Создание XLSX-файлов с результатами анализа
- **Интерактивный интерфейс**: Inline-клавиатуры для удобной работы в Telegram
- **Логирование**: Подробные логи работы бота

## 🏗 Структура проекта

```
├── bot/                    # Основной модуль бота
│   ├── handlers/          # Обработчики команд и сообщений
│   ├── keyboards/         # Inline-клавиатуры
│   ├── messages/          # Текстовые сообщения
│   └── states/            # FSM состояния
├── parser/                # Модуль парсинга данных
│   └── parser.py         # Главная логика парсера
├── analyzer/              # Модуль анализа данных
│   └── analyzer.py       # Статистический анализ
├── database/              # Работа с БД
│   └── connection.py     # Подключение к PostgreSQL
├── config/                # Конфигурация
│   └── config.py         # Загрузка переменных окружения
├── main.py               # Точка входа
└── requirements.txt      # Зависимости проекта
```

## 🔧 Технологический стек

- **Python 3.x** - основной язык
- **aiogram 3.22.0** - фреймворк для Telegram бота
- **aiohttp 3.13.2** - асинхронный HTTP-клиент для парсинга
- **SQLAlchemy 2.0.44** - ORM для работы с БД
- **PostgreSQL** - база данных
- **Pandas 2.3.3** - анализ и обработка данных
- **BeautifulSoup4 4.14.2** - парсинг HTML
- **openpyxl 3.1.5** - создание Excel-файлов

## 📦 Установка

### Требования
- Python 3.9+
- PostgreSQL
- pip

### Шаги установки

1. **Клонируйте репозиторий**
```bash
git clone https://github.com/nadenka-b/tgBotPython.git
cd tgBotPython
```

2. **Создайте виртуальное окружение**
```bash
python -m venv venv
# На Linux/Mac:
source venv/bin/activate
# На Windows:
venv\\Scripts\\activate
```

3. **Установите зависимости**
```bash
pip install -r requirements.txt
```

4. **Настройте переменные окружения**
Скопируйте `.env.example` в `.env` и заполните необходимые параметры:
```bash
cp .env.example .env
```

### Конфигурация .env

```env
# Логирование
LOG_LEVEL=INFO
LOG_FORMAT="[%(asctime)s] %(levelname)-8s %(filename)s:%(lineno)d - %(name)s - %(message)s"

# Бот Telegram
BOT_TOKEN=your_bot_token_here

# Парсер
PARSER_BASE_URI=https://abiturient.kpfu.ru/entrant/abit_entrant_originals_list

# База данных
DB_USER=postgres
DB_PASSWORD=your_password
DB_HOST=localhost
DB_PORT=5432
DB_NAME=kfu_bot
```

5. **Создайте БД**
Убедитесь, что PostgreSQL запущен и создайте базу данных с названием `kfu_bot`:
```sql
CREATE DATABASE kfu_bot;
```

6. **Запустите бота**
```bash
python main.py
```

## 💡 Использование

Когда бот запущен, отправьте любую команду в Telegram. Бот поддерживает:
- Фильтрацию программ по направлениям и уровню образования
- Анализ конкурсности по разным критериям
- Экспорт данных в формате XLSX
- Обновление данных с официального сайта КФУ

## 🗄 Архитектура БД

Бот использует PostgreSQL с ORM SQLAlchemy. Основные таблицы:
- `programs` - программы поступления
- `statistics` - статистика по программам
- `filters` - сохранённые фильтры

## 📊 Основные компоненты

### Parser (`parser/parser.py`)
Отвечает за парсинг HTML-страниц и извлечение данных о программах:
- Получение списка направлений
- Парсинг таблиц с данными
- Экстракция информации о конкурсе

### Analyzer (`analyzer/analyzer.py`)
Проводит статистический анализ:
- Вычисление конкурсности (поступающих на место)
- Группировка данных по направлениям
- Сравнение показателей

### Database (`database/connection.py`)
Управляет подключением к PostgreSQL:
- Инициализация сессий
- Создание таблиц
- Выполнение запросов

### Bot Handlers (`bot/handlers/`)
Обработчики команд Telegram:
- `/start` - приветствие
- `/help` - справка
- Inline-кнопки для фильтрации
- Обработка файлов

## 🔄 Асинхронность

Бот полностью асинхронный:
- `asyncio` для управления асинхронными операциями
- `aiohttp` для неблокирующих HTTP-запросов
- `APScheduler` для планирования задач

## 📝 Логирование

Все события логируются в консоль и в файлы. Уровень логирования настраивается через `.env`:
```python
logger = logging.getLogger(__name__)
logger.info("💚 Запуск бота обновления комбинаций фильтров...")
```

## 🤝 Вклад

Приветствуются pull requests. Для больших изменений сначала откройте issue для обсуждения.

## 📄 Лицензия

Этот проект распространяется под MIT лицензией.

## 👤 Автор

**nadenka-b** - разработка и поддержка

---

**Примечание**: Этот бот создан в образовательных целях для анализа данных о поступлении в КФУ.
